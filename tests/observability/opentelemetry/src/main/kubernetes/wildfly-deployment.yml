apiVersion: apps/v1
kind: Deployment
metadata:
  name: opentelemetry
  labels:
    app.kubernetes.io/name: opentelemetry
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: opentelemetry
  template:
    metadata:
      labels:
        app.kubernetes.io/name: opentelemetry
    spec:
      containers:
        - name: opentelemetry
          image: localhost:5000/wildfly-cloud-tests-observability-opentelemetry:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
            - name: admin
              containerPort: 9990
---
apiVersion: v1
kind: Service
metadata:
  name: opentelemetry
  labels:
    app.kubernetes.io/name: opentelemetry
spec:
  selector:
    app.kubernetes.io/name: opentelemetry
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: admin
      port: 9990
      targetPort: 9990
